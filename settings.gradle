pluginManagement {
    repositories {
        google()
        jcenter() {
            content {
                include("com.theartofdev.edmodo", "android-image-cropper")
            }
        }
        maven { url "https://jitpack.io" }
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        jcenter() {
            content {
                include("com.theartofdev.edmodo", "android-image-cropper")
            }
        }
        maven { url "https://jitpack.io" }
    }
}
rootProject.name = "RoomLogin"
include ':app'
